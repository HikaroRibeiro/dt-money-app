import { Text } from "react-native"

export const EmptyList = () => {
    return(
        <Text className="text-center text-gray-400 text-lg mt-4">
            Nenhuma transação cadastrada
        </Text>
    )
}